import type { LogoCropModalProps } from './LogoCropModal.types';

/** Native: admin belediye ekranı web’de; kırpma yok. */
export function LogoCropModal(_props: LogoCropModalProps) {
  return null;
}
